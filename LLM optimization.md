`padding`은 **입력 시퀀스(문장, 문단 등)의 길이를 일정하게 맞추기 위해** 짧은 시퀀스에 **특정 토큰(예: `[PAD]` 또는 0)**을 덧붙이는 작업입니다.

```python
문장 1: ["나는", "학교에", "간다"] → 3개 토큰
문장 2: ["너는", "밥을", "먹었니", "?"] → 4개 토큰

문장 1 → ["나는", "학교에", "간다", [PAD]]
문장 2 → ["너는", "밥을", "먹었니", "?"]
```

왜 Padding이 필요한가?
- 모델은 고정된 입력 길이를 원함
- Transformer 같은 모델은 **동시에 여러 문장을 처리(batch)** 하려면 입력 텐서의 shape이 같아야 합니다.  
- 즉, **(배치 크기, 시퀀스 길이)** 가 고정되어 있어야 하죠.
- 예를 들어, 길이가 각각 7, 10, 13인 문장 3개를 동시에 처리하려면, 가장 긴 문장인 13에 맞춰 **나머지를 padding으로 채워야** 합니다.

Batching이란?
**여러 문장을 한 번에 묶어서 모델에 넣는 작업**입니다. 모델 학습이나 추론 속도를 높이기 위해 사용합니다.
- 문장마다 길이가 다르면 텐서로 묶을 수 없습니다.
- 그래서 가장 긴 문장에 맞춰 **다른 문장들을 padding 처리**하여 동일한 길이로 만듭니다.
- 이렇게 하면 모델이 **병렬 처리**할 수 있습니다.

![[Pasted image 20250522141901.png]]

![[Pasted image 20250522141958.png]]

| 개념             | 뜻                            | 단위 예시                      | 무엇을 측정?   |
| -------------- | ---------------------------- | -------------------------- | --------- |
| **Latency**    | **요청 1건**이 **처리되는 데 걸리는 시간** | milliseconds (ms), seconds | **응답 속도** |
| **Throughput** | **일정 시간 동안 처리한 총 요청 수**      | requests/sec, tokens/sec   | **처리량**   |





|**동시 요청 수**|**최소 응답 시간**|**P50 (중앙값)**|**P90**|**P99**|**최대 응답 시간**|**성공률**|
|---|---|---|---|---|---|---|
|1|2.74s|3.40s|4.10s|4.30s|4.36s|100%|
|2|8.82s|13.80s|17.80s|18.80s|18.85s|100%|
|10|12.37s|16.80s|21.30s|22.50s|22.69s|100%|
|25|15.33s|35.00s|50.00s|51.90s|52.28s|87%|
|50|16.33s|41.00s|58.00s|59.80s|60.12s|67%|
|100|15.39s|39.00s|52.00s|54.00s|54.15s|33%|

개선된 값

