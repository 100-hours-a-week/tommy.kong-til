# 복습
$\mathbb{R}$(실수 전체 집합)은 벡터공간일까?
유클리드 공간($\mathbb{R} \times \mathbb{R} = \mathbb{R}^2$ ), 유클리드 공간($\mathbb{R}^3$)``
$$
dim \mathbb{R}^2 = 2, dim \mathbb{R}^3 = 3 \implies dim \mathbb{R} = 1
$$
벡터공간에서 차원의 정의 : 기저(집합)의 원소의 개수
$\mathbb{R}$ = 수직선(수직(직각) X), 실수를 선으로 나타낸 것 

어떤 집합이 벡터공간이라는 구조를 가지려면 

대수구조를 만들어낸 방법
자연수(0부터 시작)
	0이 있다. 
	뭔가 n 가 있으면 다음이 n+1 있다. successor, 수학적 귀납법 
정수 : 덧셈 군화
	정수를 만든 이유는?
	뺄셈을 위해서 -> 덧셈의 역원
	$\mathbb{N}$ 는 덧셈의 군이 됩니까?
		자연수에서 역원을 추가한 것이 정수 $\mathbb{Z}$
유리수 : 곱셈 군화
	나눗셈 = 곱셈의 역원을 정수에 추가 $\mathbb{Q}$
실수 : 극한 연산 -> 미적분학과 밀접한 연관
	유리수에서 확장했을까?
		실수를 유리수의 극한으로 정의(구성)합니다. 
		코시 수열의 동치류
		완비 실수 거리 공간
		미분? => 평균변화율의 극한

신수가 벡터 공간이다. 
벡터 공간에 연산이 2개 있어야한다.
1. 벡터 합 : $v,m \in \mathbb{R}^2, v + w \in \mathbb{R}^2$ (실수합)
2. 스칼라 곱 : 스칼라 필드란?
	1. 스칼라 필드도 $\mathbb{R} \implies k \in \mathbb{R}, v \in \mathbb{R} \implies k,v \in \mathbb{R}$(실수 곱)
3. 공리 8개
	1. 벡터 합 가환 군 : 실수 합 O
	2. 스칼라 곱 $k = 1 \in k*v = v \implies 1*v = v, (ab)v = a(bv) \implies abv$ O
	3. 분배법칙 $(a+b)v, a(v+w)$ 다 실수 O
			

# 선형(linear), 사상(map, mapping), 변환(transformation), 함수(function)
함수 : 집합과 집합의 연결
함수의 구성 : 이름, 정의역, 공역, 대응 규칙
어떤 함수를 함수라 완벽하게 얘기하려면 함수의 구성을 다 정의할 수 있어야 한다.
$$
y=x+1
$$
$=>$ 이름 : 익명, 정의역 : 실수, 공역 : 실수, 대응 규칙 : $y = x+1$

<span style="background:#fff88f">준동형 사상 (Homomorphism)</span>

함수 중에는 단순히 **집합의 요소를 연결하는 것**뿐만 아니라, **그 집합이 가진 구조까지 보존하는 함수**가 있다.  
이러한 함수를 **준동형 사상 (Homomorphism)** 이라고 한다.

$$
\begin{array}{r}
(G, +_G), \quad (H, +_H) \\
f: G \to H \\
\forall a, b \in G, \quad f(a +_G b) = f(a) +_H f(b)
\end{array}
$$

예를 들어, 정수 덧셈군 $(\mathbb{Z}, +)$ 에서 자기 자신으로 가는 항등 함수는 준동형 사상이다.

$$
\begin{array}{r}
(\mathbb{Z}, +), \quad (\mathbb{Z}, +) \\
f(x) = x \\
f(x + y) = x + y = f(x) + f(y)
\end{array}
$$

**<span style="background:#fff88f">군 동형 사상 (Isomorphism)</span>**

어떤 군 준동형 사상 $f$ 가 **역함수** $f^-1$ 를 가져서, 그 역함수도 역시 군 준동형 사상이면, 이를 **군 동형 사상 (Isomorphism)** 이라고 한다.

$$
\begin{array}{r}
(G, +_G), \quad (H, +_H) \\
f: G \to H \\
f^{-1}: H \to G
\end{array}
$$

즉, **군 동형 사상은 두 군이 구조적으로 동일하다는 것을 의미**한다.  
이러한 경우, 우리는 **$G$ 와 $H$ 가 "동형(isomorphic)"이다**라고 말하며, 보통 다음과 같이 표기한다.

$$
G \cong H
$$

군 동형 사상이 존재하면, $G$ 와 $H$ 는 **동일한 연산 구조를 가진다**고 볼 수 있다.


<span style="background:#fff88f">벡터 공간 준동형 사상 (선형 변환, Linear Transformation)</span>

우리가 배우는 **벡터 공간도 준동형 사상의 개념을 포함**하고 있다.  
이를 **벡터 공간 준동형 사상**, 즉 **선형 변환 (Linear Transformation)** 이라고 한다.

정의
두 벡터 공간 $(V, F, +_V, \cdot_V)$, $(W, F, +_W, \cdot_W)$ 사이의 함수 $T: V \to W$ 가 **선형 변환**이 되려면 다음 조건을 만족해야 한다.

$$
\begin{array}{r}
(V, F, +_V, \cdot_V), \quad (W, F, +_W, \cdot_W) \\
T: V \to W \\
\forall x, y \in V, \quad \forall k \in F \\[10pt]
T(x +_V y) = T(x) +_W T(y) \\[10pt]
T(k \cdot_V x) = k \cdot_W T(x)
\end{array}
$$

선형성 (Linearity) = 중첩 원리
선형 변환은 **중첩 원리 (superposition principle)** 를 따르며, 이는 다음 두 가지 성질을 포함한다.

1. **가산성 (Additivity)**  
   $$
   T(x +_V y) = T(x) +_W T(y)
   $$

2. **동차성 (Homogeneity, Scalar Multiplication Preservation)**  
   $$
   T(k \cdot_V x) = k \cdot_W T(x)
   $$

이 두 성질이 합쳐진 것을 **선형성 (linearity)** 이라고 한다.  
물리학과 공학에서 중첩 원리는 **전기회로, 양자역학, 파동 이론** 등에서 중요한 개념으로 사용된다.

**부분 공간(subspace)**
부분 구조: 부분 집핱이면서 모집합과 같은 구조를 가진 것, (모)구조를 가진 것

$$
\begin{gathered}
(\mathbf{Z},+)  \text{: 정수 덧셈군}\\
\mathbf{Z}_2 \subset \mathbf{Z} \text{:짝수}\\
\left(\mathbf{Z}_2,+\right) \text{:짝수 덧셈군}\\
\mathrm{Z}_2<\mathbf{Z} \\
(\text { 부분군 ) }
\end{gathered}
$$
모구조랑 같은 구조를 부분구조도 가질 수 있다. 

**부분 공간: 부분 집합이면서 모(벡터)공간과 같은 구조(즉 같은 연산)인 것.**
$$
\mathbb{R}^2
$$
$=>$ 의 부분 공간? $RR$ 
부분 공간이 되려면 3가지 공리를 따라야함.
$$
\begin{aligned}
&(V, F,+, \cdot), W \leq V \\
& \Longleftrightarrow \\
& 1: 0 \in W \\
& 2: \forall x, y \in W, x+y \in W \\
& 3: \forall x \in W, \forall c \in F, c x \in W
\end{aligned}
$$

**차원 정리**

$$
\begin{aligned}
T: V \rightarrow W, 선형변환 \\
N(T)=\{x \mid T(x)=0, x \in V\}(\text { 영공간(null space, kernel) }) \subset V \\
R(T)=\{T(x) \mid x \in V\}(\text { 상공간(image,range) }) \subset W \\
N(T)<V, R(T)<W \\
\operatorname{nullity}(T)=\operatorname{dim}(N(T))) \\
\operatorname{rank}(T)=\operatorname{dim}(R(T)) \\
\operatorname{dim}(V)=\operatorname{nullity}(T)+\operatorname{rank}(T)
\end{aligned}
$$
**선형 변환과 부분공간**
선형 변환에서 중요한 개념으로 **영공간(null space)** 과 **상공간(range, image space)** 이 있다.  
이 두 공간은 각각 정의역과 공역 내에서 부분공간을 이루며, 선형 변환이 어떻게 작용하는지를 설명하는 중요한 역할을 한다.
선형 변환에서 정의역의 부분집합 $V$ 중에서 선형 변환 후 **$0$이 되는 원소들의 집합**을 **영공간**(Null Space)이라고 한다.
$$
\text{Null}(T) = \{ x \in V \mid T(x) = 0 \}
$$
영공간의 차원을 **nullity**라고 하며, 이를 $\text{nullity}(T)$로 표기한다.

공역 $W$ 내에서 선형 변환 $T$에 의해 생성된 원소들의 집합을 **상공간(Image Space, Range)** 이라고 한다.
$$
\text{Im}(T) = \{ T(x) \mid x \in V \}
$$
상공간의 차원을 **rank**라고 하며, 이를 $\text{rank}(T)$로 표기한다.
영공간과 상공간은 각각 부분공간을 형성한다.  

선형 변환은 $V$와 $W$라는 두 공간을 분해하는 역할을 하며, 이를 통해 **일부를 부분공간으로 분리**할 수 있다.
Rank-Nullity 정리에 따르면  
$$
\text{nullity}(T) + \text{rank}(T) = \dim(V)
$$
즉, 선형 변환에서 **영공간의 차원과 상공간의 차원의 합이 원래 정의역 공간 $V$의 차원과 동일**하다.

우리가 하는 **AI 모델**도 결국 하나의 **선형 변환(선형 사상)** 이다.  
즉, **입력(Input)** 과 **출력(Output)** 을 선형 변환을 통해 연결하는 것이다.  
여기서 중요한 점은 **입력 차원이 함수가 이뤄내는 부분공간의 차원과 밀접하게 관련**된다는 것이다.
우리가 원하는 것은 다음과 같이 **현실과 수학적 모델을 연결하는 함수**를 찾는 것이다.
1. **실제 (Real World)**  
   - 현실에서 데이터를 수집  
2. **가상 (Mathematical Model)**  
   - 선형 변환을 포함한 AI 모델이 현실 데이터를 학습  
3. **미래 (Prediction)**  
   - 학습된 모델이 새로운 입력에 대해 적절한 출력을 예측  
즉, **입력과 출력 사이의 관계를 명확히 설명할 수 있는 선형 변환 모델을 찾는 것** 이 우리의 목표이다.

**기저로 표현**
기저가 왜 필요한가? 
기저의 개수가 차원이랑 동일하다. 

순서 기저와 벡터의 표현(좌표 벡터)

$$
\begin{array}{r}
A = \{1,2\} = \{2,1\} \\
\beta=\left\{u_1, u_2, \cdots, u_n\right\} \text{(순서 기저(원래 집합이지만 순서가 필요해서 순서가 있다고 가정한다.))}\\
\mathbb{R} => \beta = \{(1,0),(0,1)\} = \{e_1,e_2\} \\
v \in V, \exists! \text{(유일하게 존재한다.)} \left\{a_n\right\}, v=\sum a_i u_i \\
(2,3) \in \mathbb{R}^2 \implies (2,3) = 2(1,0) + 3(0,1) \implies   v = \begin{bmatrix} 2 \\ 3 \end{bmatrix} \\
(2,3) \in \mathbb{R}^2 \implies (2,3) = 2(1,0) + 1(0,1) \implies   [v]_{\beta} = \begin{bmatrix} 2 \\ 1 \end{bmatrix} \\
\text{같은 벡터 공간에서 두 개의 서로 다른 기저를 사용하여 좌표 벡터를 만들 수 있다.} \\ 
\text{이 경우 좌표 벡터의 표현은 다를 수 있지만, 근본적으로 동일한 벡터를 나타낸다.} \\ 
\text{즉, 기저는 다를 수 있지만 벡터 자체는 변하지 않는다.} \\
{[v]_\beta=\left[\begin{array}{c}
a_1 \\
a_2 \\
\vdots \\
a_n
\end{array}\right]} \\
\text{우리가 가장 찾고 싶은 기저는 바로 표준 기저이다.} \\ \text{이를 계산하는 과정에서 고유값과 고유벡터를 활용한다.}\\
\end{array}
$$

**선형 변환의 (행렬) 표현**

모든 선형 변환은 반드시 행렬로 표현할 수 있을까? → 가능하다.  
선형 변환을 행렬로 나타낼 수 있다는 것은 매우 중요한 의미를 가진다.  

**AI 모델(함수) = 선형 변환**  

선형 변환이 가능하다는 것은 곧 계산이 가능하다는 의미이다.  
즉, 선형 변환을 행렬로 나타낼 수 있다면, 수치적으로 효율적인 연산이 가능해진다.  

예를 들어, **신경망에서의 선형 연산**은 가중치 행렬을 사용하여 계산되며, 이는 벡터와 행렬의 곱셈으로 표현된다.  

행렬 표현을 통해 **선형 변환의 다양한 성질을 쉽게 분석**할 수 있으며,  
역행렬, 특이값 분해(SVD), 고유값 분해(Eigendecomposition) 등의 수학적 도구를 활용할 수 있다.  

특히, **머신러닝과 최적화에서 선형 변환을 행렬로 나타내면 미분, 역전파(backpropagation) 등의 계산이 간단해진다.**  

따라서 **선형 변환을 행렬로 표현하는 것은 단순한 형식적인 변환이 아니라, 실제 계산과 이론적 분석 모두에서 필수적인 과정이다.**  

$$
\begin{array}{r}
\beta=\left\{v_1, v_2, \ldots, v_n\right\}(V \text { 의 기저 }) \\
v \in V \to [v]_{\beta} \\
\gamma=\left\{w_1, w_2, \ldots, w_m\right\}(W \text { 의 기저 }) \\
w \in W \to [W]_{\gamma} \\
T: V \to W \\
T(x,y) = (2x,2y) \\ 
T : \mathbb{R} ^ 2 \to \mathbb{R} ^ 2 \\
v = (x,y), w = (2x,2y) \\ 
T\left(v_j\right)=\sum_{i=1}^m a_{i j} w_i \\
2 \times 2 \text{로 표현될 수 있는데 각각의 원소가 어떻게 생겼을까?} \\
A=\left[a_{i j}\right]=[T]_\beta^\gamma \\ 
A =
\begin{pmatrix} 
a & b \\ 
c & d 
\end{pmatrix}
 \\
v_1 = (1,0), v_2 = (0,1), w_1 = (1,0), w_2 = (0,1) \\
T(v_1) = T(1,0) = (2,0) = 2w_1 + 0w_2 = a_{11}w_1 + a_{12}w_2 \\ 
T(v_2) = T(0,1) = (0,2) = 0w_1 + 2w_2 = a_{21}w_1 + a_{22}w_2 \\
A =
\begin{pmatrix} 
2 & 0 \\ 
0 & 2 
\end{pmatrix}
 = [T]
\end{array}
$$
$$
\begin{array}{r}
P^n : n \text{차 이하의 다항식} \implies (n+1) \text{차원} \\

P^2 : 2 \text{차 이하의 다항식} \implies ax^2 + bx + c \implies \text{기저가 3개} \implies \text{3차원} \\

P^{\infty} : \text{무한 차원의 기저로 모든 다항식을 표현} \implies \text{테일러 급수} \\

\text{다항 공간을 바탕으로 모델링이 가능하다.} \\

\text{기저가 삼각함수로 구성된 경우} \implies \text{푸리에 급수}
\end{array}
$$


결론
**선형대수는 기저 놀음이다.**
---
질문

